The example directory contains files used for testing some
kinds of interaction with the crank. The json-* files contain
JSON data which would normally be generated by the webserver
that is using the crank as an FCGI program. The Makefile
creates a shorthand for various "commands" that you could
sensibly send to the crank: start (to connect to the LDAP
server), stop, query ...

