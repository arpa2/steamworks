# - Config file for the Steamworks package
# It defines the following variables
#  Steamworks_INCLUDE_DIRS    - include directories for Steamworks
#  Steamworks_LIBRARIES       - libraries to link against (but there are none)

# Compute paths
get_filename_component(Steamworks_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

set(Steamworks_LIB_DIRS "@CONF_LIB_DIRS@")
set(Steamworks_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# No libraries as yet
#
# set(Steamworks_LIBRARIES -L${Steamworks_LIB_DIRS} steamworks)
# set(Steamworks_STATIC_LIBRARIES ${Steamworks_LIB_DIRS}/libsteamworks.a)

